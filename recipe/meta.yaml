{% set name = "sybil" %}
{% set version = "10.1.0" %}
{% set sha256 = "062249c8886a0ab19e45d1c3afd5631ec806e7a95cf5153c96560f5e47756cbd" %}
{% set python_min = "3.11" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - pip
    - hatchling
  run:
    - python >={{ python_min }}

test:
  imports:
    - sybil
  requires:
    - pip
    - python {{ python_min }}
  commands:
    - pip check

about:
  home: https://github.com/simplistix/sybil
  summary: Automated testing for the examples in your documentation.
  description: |
    This library provides a way to check examples in your code and documentation
    by parsing them from their source and evaluating the parsed examples as part
    of your normal test run. Integration is provided for the main Python test
    runners.
  license: MIT
  license_file: LICENSE.txt
  doc_url: https://sybil.readthedocs.io/
  dev_url: https://github.com/simplistix/sybil

extra:
  recipe-maintainers:
    - joaander
